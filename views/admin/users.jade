extends admin_layout

block content
    .container-fluid
      .row
        .col-xs-12.col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2.col-lg-6.col-lg-offset-3
          .panel
            .panel-body
              .container-fluid
                #error.hidden.alert.alert-danger(role='alert')
              h3 List of OEIS Users
              br
              p
                for user in users
                  if user.admin
                    h4=user.name
                      span!= " <i>(" + adminNumToWord(user.admin) + ")</i>"
                  else
                    h4=user.name
                  for value, item in user
                    if item != "name" && value != undefined && value != "" && item != "admin"
                      span!="<b>" + item + "</b>"
                      span=": " + value
                      span!="<br>"
                  br
                  br
